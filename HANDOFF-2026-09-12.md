# Handoff — Recast Books, 2026-09-12

Paste this into a new Claude Code session opened at `~/Desktop/Claude` (or directly in
`~/Desktop/Claude/Recast-books`).

---

You are continuing the build of **Recast Books**, the new bookkeeping system for Recast
Properties LLC, in the repo `~/Desktop/Claude/Recast-books/`. Start by reading, in order:
`Recast-books/CLAUDE.md` (status + load-bearing constraints), `docs/decisions.md`
(D-001..D-012 — never reverse one without a new entry), `docs/phase2-spec.md` §11 (gate
progress), and `CHANGELOG.md` (latest entries). `BUILD-PLAN.md` §7 is the phase list.
My memory file `recast-books-build` has the live ids and gotchas.

**Where things stand**
- Phases 0 and 1 are built and gated: workbook, writer, posting engine, Google sign-in,
  properties, Dennis's advances with the accrual engine, reports.
- Phase 2, the receipts bookkeeper, is built and **live in parallel** since 2026-09-11:
  the Gmail poller reads receipts@/travel@ mail dated 2026-09-11 onward, Claude Opus 5
  decides with tools, `lib/gate.mjs` enforces the rails, posts go to the NEW workbook
  only, documents are filed in Drive under "Recast Books". The old receipts system in
  `../Recast-site/` keeps running untouched and must never be modified.
- Phase 2 gate: dry run over 30 days reviewed by Paul; live receipts post correctly with
  the right paying account. Still open: (1) Paul photographs a real receipt on the
  Upload page from his phone and it posts end to end — confirm Journal rows and the
  Drive file; (2) confirm the 3 AM "Books" digest email arrived. Then record the gate as
  passed in `docs/phase2-spec.md` §11 and `CHANGELOG.md`.
- Phase 3 (Plaid banking) is next, on Paul's explicit "go". Plaid production access was
  requested 2026-09-11 (2–3 days); when approved, Paul sets `PLAID_CLIENT_ID` and
  `PLAID_SECRET` on the Netlify site himself.
- Paul still owes Dennis's and the accountant's Google emails for the Users list.

**How Paul wants to work**
- **One step at a time.** When Paul must do something (console, Apps Script editor,
  Terminal, phone), give exactly one step and wait for "done". He gets annoyed by lists.
- Never handle his secrets: he runs `npx netlify-cli env:set … --secret …` himself.
- Claude is the bookkeeper: model judgment decides; code owns arithmetic, balanced
  entries, txn_id identity, gates. Every model verdict must have an action downstream,
  and a rail must never hold a document while ignoring a fact the system already has
  (D-012). When something is held that should not be, fix the rail, don't add a rule.
- Confirm plans before code; build phases with Sonnet agents from a written spec
  (`docs/phaseN-spec.md`), review every file yourself, run `npm test`, deploy with
  `npm run deploy`, gate through Paul's browser (claude-in-chrome; he stays signed in on
  the netlify.app origin), then ask for the next go.

**Operating cheatsheet**
- Deploy site: `cd ~/Desktop/Claude/Recast-books && npm run deploy`.
- Writer: `cd apps-script/writer && clasp push -f && clasp deploy -i AKfycbwRH2juzUkuOzmltG79lbWyQ5CH-Q_cZukY7WcvspOwIw8EpCTnbjQforU-A-q2Jljy`.
  Manifest scope changes need Paul to re-run `setup()` in the editor.
- Poller: `cd apps-script/poller && clasp push -f` (no deploy; runs latest saved code).
- Inbox API from Paul's browser: `GET /api/inbox?status=all`, `POST /api/inbox
  {action:"reprocess"|"approve"|"dismiss"|"delete", docId}`. Envelopes carry `model`,
  `gate`, `error`, `error_stack`. Netlify CLI logs are unreliable — read the envelope.
- Prompt lives in `lib/bookkeeper-prompt.md`; after editing run
  `node scripts/build-prompt.mjs` (a test enforces sync).
- If clasp says `invalid_grant`, Paul runs `clasp login` (one step).

**First thing to do in the new session:** ask Paul whether Step 7 (phone upload) is done
and whether the 3 AM digest arrived; verify both; close the Phase 2 gate in the docs;
then ask for the go on Phase 3 and write `docs/phase3-spec.md` (Plaid Link, transactions
sync into the Feed tab, Claude-driven matching against receipts/journal, per-account
monthly reconciliation, statement PDF upload as fallback — see `BUILD-PLAN.md` §3–§7).
