# Handoff, 2026-09-23 - Recast Books: live on the real books; loose ends after cutover

Replaces `HANDOFF-2026-09-22.md`. Standing rules 1-8 of `HANDOFF-2026-09-21.md` still hold.

Work only in /Users/paulbjork/Desktop/Claude/Recast-books/ (own git repo, master, no remote). The old workbook
"Recast 2026" is CLOSED (frozen 2026-09-21); never write to it. `../Recast-site/` is not touched from here
(its receipts poller's triggers were deleted 2026-09-22 with Paul's permission - nothing else changed there).

READ: `CLAUDE.md` status block, `docs/phase4-audit.md` §51-§54, `docs/cutover-runbook.md` (§5-§6 open).

STATE. Production workbook "Recast Books" holds the migrated books (1,048 entries, $240,844.35, 922 Drive
links, 33 advances - tied out both paths, §51) plus the live receipts since 09-17 (20 read on 09-21/22).
`WRITER_URL` = production. Only the "Books |" digest arrives. Property tabs reconciled to the old workbook
(§54); Ashburne is the one "Heavy" tab, laid out as Paul drew it on 09-22 - one-off, he will not use that
format again.

OPEN, in order:
1. Inbox history leftovers: several hundred `pending` envelopes from the 09-17 reads are migration
   leftovers (their rows are in the Journal). List them, Paul agrees, dismiss in bulk (runbook step 15).
   Also two live cards for Paul: Target gift cards $300, Alaska Airlines $166.
2. properties@ poller: designed in Phase 2.6, never switched on (audit §9). Paul signs in as properties@,
   creates the project from `apps-script/poller/`, `MAILBOX=properties`, `START_DATE=2026-09-17`. Until then
   property-address mail is not read; Paul sends to receipts@ with the property name first.
3. Old workbook: delete RECAST BIZ / Materials rows 5-6 (Amazon $249.98, $10.81 - duplicates the old poller
   wrote on 09-21), then Share → Viewer, rename CLOSED (runbook step 16-18); staging workbook → ARCHIVED.
4. Two Journal lines lack their Drive link after the misfire: Wi-Fi Onboard $8.00 (receipt-20260918-d5a5…)
   and Anthropic $13.06 (gm-1a0c5f8e100f44d8, card still "error"); relink by hand.
5. Phase 5 sell wizard (D-033; Granite and Sparkling are the closed sales), then Phase 3 banking.

TOOLS / GOTCHAS: `clasp` login expires every few days (Paul: `npx clasp login`); after a push, verify by
pull. `WRITER_SECRET` and `POLLER_SECRET` are masked on Netlify - scripts that need them run in Paul's
Terminal (`recover-errored.mjs`, `migration-file-docs.mjs`). `books-cache` snapshots lag a few minutes.
Claude cannot create files in Paul's Drive (renames/searches are fine). The doGet misfire is handled in the
client; watch for `REDIRECT_MISFIRE` on writes (never retried - the caller decides).
