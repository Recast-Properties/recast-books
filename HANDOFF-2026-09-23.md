# Handoff, 2026-09-23 - Recast Books: live on the real books, Phase 5 shipped; sign-offs then Phase 3

Rewritten 2026-09-22 evening (the earlier version of this file was written that morning and its open items
1-4 were all finished the same day - audit §55-§60). Standing rules 1-8 of `HANDOFF-2026-09-21.md` still
hold; rules 1, 2, 6 and 7 are the live ones now that the migration is done.

Work only in /Users/paulbjork/Desktop/Claude/Recast-books/ (own git repo, master, no remote). The old
workbook "Recast 2026 CLOSED 2026-09-21" is frozen and view-only; never write to it. `../Recast-site/` is
not touched from here (its receipts poller's triggers were deleted 2026-09-22 with Paul's permission -
nothing else changed there).

READ: `CLAUDE.md` status block, `docs/phase4-audit.md` §51-§64, `docs/phase5-spec.md`, `docs/decisions.md`
D-035 … D-042 (D-042 is the light property tab, and `docs/phase2.6-spec.md` §5 is its layout of record). `docs/cutover-runbook.md` is done but for step 17 (optional).

STATE. Production workbook "Recast Books" holds the migrated books (1,048 entries, $240,844.35, 922 Drive
links, 33 advances - tied out on both paths, §51), the live receipts since 09-17, and both closed sales.
After Phase 5: **2,275 live rows, debits = credits at 4,457,856.90**; 4000 = 705,000.00, 5000 = 619,945.68,
1510 = 0.00, 2000 = 0.00, 9010 = 85,054.32; 17 sale entries. 453 tests. `WRITER_URL` = production, web app
@4. Property tabs reconciled to the old workbook (§54); Ashburne is the one "Heavy" tab, laid out as Paul
drew it on 09-22 - a one-off, he will not use that format again. Both pollers run: receipts@ and properties@
(project `1k2htSsuL…RoTZ`, `MAILBOX=properties`, `START_DATE=2026-09-17`, push with `clasp push -f -P
.clasp-properties.json`). Inbox holds 18 documents on purpose: 12 parked Home Depot / Lowe's for Phase 3 and
Paul's 6 live cards. Sold = **1616 Granite and 280 Sparkling only**; 881 Newport is under contract (Bison
Title #260910) and 104 Ashburne is held.

**2026-09-23, two faults Paul found and what came of them (audit §61-§63).** (a) The heavy tab was writing
every refresh into its own spacer columns: `setupPropertyTab` inserts the left spacer column AFTER writing
the grid, the light template's `deleteColumn(8)` used to cancel that, and `8b5138b` made the delete
`!heavy` so nothing cancelled it - `refreshHeavyBlocks_` kept writing at the built column, one left of the
header, so new lines landed in the spacer and the blocks went stale. A block's column is now read from its
own header; an untraded Holding line falls under Utilities. The totals were never wrong. (b) The Inbox card
(D-040, D-041): bullets are actions and nothing else, property is per item with Approve splitting one entry
per property, the account picker is grouped business/property, a live D-010 check, and a trade picker. All
pushed to the writer and verified by pull; no deploy was needed (`doPost` unchanged). 455 tests.

**2026-09-23, the light property tabs (audit §64, D-042).** Four changes Paul asked for going through 469
Brushwood, all on the **light** template - 104 Ashburne, the one Heavy tab, is untouched by every one.
(a) **The Dennis commission rows are gone** from Payouts - his `Dennis commission (x% of sale)` and Paul's
`Less Dennis commission`: "he will never charge commission for these". On a partnership deal his return is
principal + interest + his 50% share; the commission was only ever a **bank-deal** term and that path is
untouched (`Properties.dennis_commission_pct`, the heavy tab's own line, `lib/sale.mjs`'s 1210 entry at
closing, D-036) - which is what Ashburne settles on. (b) **`Due to Paul (paid less reimbursed)` →
`Paul Paid (direct)`**, label only. (c) **`Concession (type it here)`** below Closing % in the Profit
Breakdown - the heavy tab's cell, blue, typed, kept across rebuilds; Net Profit subtracts it as `-ABS(...)`
so a hand-typed minus cannot turn a credit into profit. (d) **`Received (advances, refunds)` split** into
`Received (advances)` + `Received (refunds)` on payee - both lines of an advance carry `Dennis Little`
(`buildAdvance`), a refund is a negative cost row carrying its vendor. It is an **exhaustive partition** on
purpose: a payee the rule does not expect can only move a line between the two rows, never out of the block
total. Checked against the books before writing it (33/33 advances `Dennis Little`; all 7 negative rows are
vendors; Brushwood's `-$54.09` is the Home Depot REFUND on 1401). **Dennis Paid (direct) keeps ONE Received
row**, unfiltered - a direct-paid Dennis cost *is* an advance, so an advances row there could only read zero;
shipped with all three split, flagged it, Paul agreed and it came back off. 455 tests, pushed, no deploy.
`docs/property-tab-anatomy.md` is deliberately not updated - it documents the **old** workbook's tab.

OPEN, in order:
0. **One step waiting on Paul: Extensions -> Apps Script -> run `rebuildAllPropertyTabs`.** One editor Run
   does both open tab jobs: it puts the 09-23 light-tab changes (D-042) on all ten tabs, and it rebuilds 104
   Ashburne, which is what clears the text already sitting in its spacer columns and brings the Energy Texas
   line in under Utilities. Typed cells survive a rebuild (Sale Price and Concession everywhere, plus
   Ashburne's Agent Commission %). Until it runs, the tabs still show the 09-22 state. The per-tab menu item
   (Recast Books -> Rebuild property tab) does one tab at a time; there is no bulk menu item.
0b. ANSWERED 2026-09-23 - **"tools are overhead"**: D-010 stands, no new account. The prompt now says a
   tool is overhead even when bought for one job, and that a hardware receipt mixing tools and materials is
   two entries (6510 OVERHEAD + 1030 on the property). **That prompt change is NOT deployed** - it is a
   `npm run deploy`, Paul's step, and nothing else is waiting on it.
1. **Closing-tab sign-off (Paul).** The closing statement is written beside the property tab as
   `<property> - Closing` while the layout is proved; when he signs it off, set `CLOSING_TAB_IN_PLACE = true`
   (`apps-script/writer/Menu.gs:1048`) and it is written onto the property tab itself, as the old workbook
   did ("1616 Granite RECONCILED"). Labels he types on a settlement line or a cost row survive a rebuild.
2. **Newport and Ashburne through the same wizard when they close.** Ashburne is the bank deal - 12%, 3%
   commission, no profit share (D-030, D-038: the two closed deals were 8%, held properties stay 9% as a
   buffer).
3. **Runbook step 17 (P, optional):** drag `2026/` and `Migration evidence/` from the "Recast Books STAGING"
   Drive folder into "Recast Books". File links survive a move; the Journal is untouched.
4. **Phase 3, banking** (`docs/phase3-spec.md`, to be revised for the menu shape; D-019 dropped Plaid -
   statements are uploaded). Everything parked lands here: the 12 Home Depot / Lowe's receipts, the Cotality
   invoices, the 128 rows / $51.4K with no document ($36,880 of it accepted by Paul).
5. **W-9 collection.** Nine payees over the 2026 threshold, zero W-9s on file, January 31 statutory deadline,
   no dependency on any phase - the one genuinely urgent item (`data/vendors-1099-2026.md`).

Owed by others: Dennis's PayPal receipts for the Granite, Sparkling and Newport $299 listing fees.

TOOLS / GOTCHAS: **never edit the writer by replacing a block on a text match** (it has deleted a live
function twice; the three `writer-gs-lint` tests catch it) and **never `git checkout` a file to undo a
scratch experiment** - it takes uncommitted work with it (done once here, 2026-09-23, and redone from the
edit script). A sidebar change needs the Inbox closed and reopened, not a deploy. `clasp` login expires
every few days (Paul: `npx clasp login`); after a push, verify by
pull. `npm run deploy` is Paul's step; the writer takes `clasp push -f`, and `clasp deploy -i AKfycbxNisU…`
only when `doPost` changes. `WRITER_SECRET` and `POLLER_SECRET` are masked on Netlify - scripts that need
them run in Paul's Terminal (`recover-errored.mjs`, `migration-file-docs.mjs`). `books-cache` snapshots lag a
few minutes - check `posted_at`, not only `fetchedAt`. Claude cannot create files in Paul's Drive
(renames/searches are fine). `REDIRECT_MISFIRE`: under concurrent calls the writer's POST redirect lands on
`doGet`; `lib/writer-client.mjs` retries reads, never writes - the caller decides. **Never edit the writer by
replacing blocks on a text match** (it deleted a live function twice and duplicated one once, §60); the three
`writer-gs-lint` tests catch it. A settlement read is a background job - a synchronous function is cut off at
ten seconds. Every bookkeeper call carries the server-side refusal fallback (`fallbacks: "default"`) after
the `reasoning_extraction` refusals of §56.
