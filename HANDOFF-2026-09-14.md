# Handoff — Recast Books, end of 2026-09-14

Paste this into a new Claude Code session opened at `~/Desktop/Claude` (or directly in
`~/Desktop/Claude/Recast-books`).

---

You are continuing the build of **Recast Books**, the new bookkeeping system for Recast
Properties LLC, in the repo `~/Desktop/Claude/Recast-books/`. Start by reading, in order:
`Recast-books/CLAUDE.md` (status + load-bearing constraints), `docs/decisions.md`
(D-001..D-017 — never reverse one without a new entry), `docs/phase2.5-spec.md` (the read
cache, shipped today), and `CHANGELOG.md` (latest entries). `BUILD-PLAN.md` §7 is the
phase list. My memory file `recast-books-build` has the live ids and gotchas.

**Where things stand**
- 2026-09-14: compared the night-of-09-13 digests of the old and new bookkeepers (Paul:
  "the original understood the receipts better"). Fixed: invoice-keyed `txn_id` so twins
  collide in the writer's lock (Anthropic had posted twice); attachments labelled
  octet-stream typed by extension (Netlify PDFs); poller behaviours ported from
  `receipts-poller.gs` (inbox-address check, attachment cap) — lesson: copy working code,
  don't re-spec it. Sign-in hang fixed: `books-warm-background` refreshes every tab's
  snapshot, kicked by the poller every 15 min (Netlify's schedule did not fire); TTL 20
  min. **Totals** tab added to the workbook (writer `setupTotals()`, formula-only, ties to
  the app's TB). Accounts 6350/6930 are live. Duplicate Anthropic voided; Netlify $20
  approved with its payer (D-014 flow works).
- Later on 2026-09-14: **D-015** recorded (overhead locks by period; a property locks at
  sale with a Dennis interest true-up; post-sale bills to COGS under the sold property;
  Dennis's share on a per-partner adjustment balance settled at the next payout).
  **D-016**: Dennis's rate is 8%, not 9% (fitted from two of his own figures; Settings,
  code defaults, writer seed all 0.08; accrual goldens pinned to 9% explicitly). **Voided
  pairs hidden**: Journal page hides an entry + its reversal by default (Show link);
  Totals gross columns exclude them via a helper column (Paul reran `setupTotals`;
  verified TB 498.02 both sides, nets unchanged).
- Phases 0, 1 and 2 are built and gated. The receipts bookkeeper is **live in parallel**
  since 2026-09-11 for receipts@/travel@ mail, posting to the NEW workbook only; the old
  system in `../Recast-site/` keeps running untouched and must never be modified.
- Phase 2 gate closed today: phone upload verified end to end (FedEx Office $12.75,
  Journal rows 32–33, Drive file), first 3 AM digest received.
- Decisions today: **D-013** the new workbook is cleared once at the start of Phase 4,
  then history is migrated and live receipts replayed; append-only after that. **D-014**
  an unidentified payer is never defaulted — the model reports `paid_from = UNKNOWN`,
  the gate holds with `PAYER_UNKNOWN`, Paul assigns the account on the Inbox card.
  Settings `default_paid_from_*` are no longer read. The FedEx entry stays on 1402 by
  Paul's choice (it goes with the D-013 clear).
- **Phase 2.5** shipped: every workbook read goes through `readTab` (Blobs `books-cache`
  snapshots, Journal 60 s / others 10 min, refreshed by every write handler for the tab
  it wrote, `fresh: true` for the model's ledger view and the D-012 duplicate re-check,
  stale-beats-dead under an 8 s timeout). Warm reads 0.2–0.25 s, down from 2–13 s.
  Filed documents are now named `<date> <vendor> <total>.<ext>`.
- Paul asked whether Sheets should remain the system of record. Answer given and
  accepted: Sheets stays; if a database swap ever happens it belongs at the Phase 4
  clear, not before Plaid. No decision entry needed unless he reopens it.
- **Phase 3 (Plaid banking) is next and is blocked on Plaid production keys** (requested
  2026-09-11). When they arrive Paul sets `PLAID_CLIENT_ID` and `PLAID_SECRET` on the
  Netlify site himself; then write `docs/phase3-spec.md` (Plaid Link, `/transactions/sync`
  into the Feed tab, Claude-driven matching against receipts/journal, per-account monthly
  reconciliation, statement PDF upload as fallback — `BUILD-PLAN.md` §3–§7, D-007) for
  Paul's review before code.
- Paul still owes Dennis's and the accountant's Google emails for the Users list.
- Cosmetic: the Dashboard phase card still says "Phase 0".

- **Phase 2.6 built and deployed (2026-09-14 afternoon)** — `docs/phase2.6-spec.md`: property
  mail lives in properties@ (each property email is a Google Group + a Gmail label made by
  the Recast-site email tool); a second copy of the poller runs as properties@
  (`MAILBOX=properties`) and registers mailboxes from those labels; upload accepts a
  property name as channel; the model is told which property mailbox a document came
  through; writer `propertyTab`/`setupPropertyTab(name)` builds a formula-only tab per
  property; the Properties add form offers the labels as a dropdown and builds the tab on
  save. **D-017**: status is Held or Sold only (optional `contract_price` feeds the
  preliminary payout; sold leaves the allowlist). Cache guard: a writer read without rows
  is never stored or served (an empty Journal snapshot 502'd the reports once).
- **1616 Granite exercise, in progress:** the property is registered (held, 279,001 on
  2026-04-07) and its tab exists. Remaining Paul steps, one at a time: (1) three advances
  on the Dennis page — $279,001 2026-04-07 "purchase principal", $5,500 2026-06-01,
  $1,338 2026-06-05; (2) the properties@ poller: sign in as properties@ at
  script.google.com, new project "Recast Books Poller — properties", paste
  `apps-script/poller/Code.gs` and the manifest scopes from `appsscript.json`, script
  properties `MAILBOX=properties`, `POLLER_SECRET` (same value as the paul@ poller),
  `BOOKS_UPLOAD_URL=https://books.recast-properties.com/api/upload`,
  `START_DATE=2026-04-01`, run `setup()` (installs only the 15-min poll, no digest);
  (3) `dryRunBatch` with DRY_QUERY over the Granite label, review the cards; (4) live.
  Gate is `docs/phase2.6-spec.md` §7.

**How Paul wants to work**
- **One step at a time.** When Paul must do something (console, Apps Script editor,
  Terminal, phone), give exactly one step and wait for "done". He gets annoyed by lists.
- Never handle his secrets: he runs `npx netlify-cli env:set … --secret …` himself.
- Claude is the bookkeeper: model judgment decides; code owns arithmetic, balanced
  entries, txn_id identity, gates. Every model verdict must have an action downstream,
  and a rail must never hold a document while ignoring a fact the system already has
  (D-012). A fact only Paul knows (who paid) is held for him, never guessed (D-014).
- Confirm plans before code; build phases with Sonnet agents from a written spec
  (`docs/phaseN-spec.md`), review every file yourself, run `npm test`, deploy with
  `npm run deploy`, gate through Paul's browser (claude-in-chrome; he signs in on the
  netlify.app origin — the custom domain has no session), then ask for the next go.
- The permission classifier blocks JS `fetch` writes to the app from Claude's browser
  tab; reads are fine. Writes are Paul's step in the UI.

**Operating cheatsheet**
- Deploy site: `cd ~/Desktop/Claude/Recast-books && npm run deploy`.
- Writer: `cd apps-script/writer && clasp push -f && clasp deploy -i AKfycbwRH2juzUkuOzmltG79lbWyQ5CH-Q_cZukY7WcvspOwIw8EpCTnbjQforU-A-q2Jljy`.
  Manifest scope changes need Paul to re-run `setup()` in the editor.
- Poller: `cd apps-script/poller && clasp push -f` (no deploy; runs latest saved code).
- Inbox API from Paul's browser: `GET /api/inbox?status=all`, `POST /api/inbox
  {action:"reprocess"|"approve"|"dismiss"|"delete", docId}`. Envelopes carry `model`,
  `gate`, `error`, `error_stack`. Netlify CLI logs are unreliable — read the envelope.
- `GET /api/meta?tab=<Tab>&fresh=1` (owner) forces a re-read past the cache.
- Prompt lives in `lib/bookkeeper-prompt.md`; after editing run
  `node scripts/build-prompt.mjs` (a test enforces sync).
- If clasp says `invalid_grant`, Paul runs `clasp login` (one step).

**First thing to do in the new session:** continue the 1616 Granite exercise at whichever
step Paul is on (ask), then ask whether the Plaid production keys have arrived and whether he has set them on Netlify. If yes, write `docs/phase3-spec.md`
and ask for his review. If no, ask whether anything on the live site felt slow or stale
since Phase 2.5, and otherwise wait.

- **Evening 2026-09-14:** property tab rebuilt to match the old workbook's tab side by
  side (summary / Dennis / Rehab Costs / Utilities with Paul-Dennis-Recast checkboxes,
  payout tie-out row); `Properties.tax_annual` + Jan-1-to-date proration until sold;
  `rebuildAllPropertyTabs()` editor helper (Paul had not yet run it for 1616 Granite).
  **D-018**: Anthropic Console workspaces Recast Books / Receipts (old site) / Title
  Search / Anything else, each system on its own key (books key non-expiring; old Personal
  key expires 2026-10-11 and can be deleted); `1520 Prepaid API credits` for top-ups;
  `/api/api-costs` + Settings page section preview/post usage by workspace; paul@ poller
  calls it on the 2nd. `ANTHROPIC_ADMIN_KEY` set. August 2026 preview: $231.67 all Default
  (pre-workspace); October is the first clean month. Netlify gotcha: `env:set --secret`
  needs `--context production`.
