# Handoff, 2026-09-22 - Recast Books Phase 4: everything rehearsed, cutover day next

Replaces `HANDOFF-2026-09-21.md` (its standing rules 1-8 still hold - read them there).

Work only in /Users/paulbjork/Desktop/Claude/Recast-books/ (own git repo, master, no remote). Never touch the
old workbook, ../Recast-site/, or the production workbook/writer without Paul saying so.

READ: `CLAUDE.md` status block, **`docs/cutover-runbook.md`** (the day, step by step, with checks), audit
§45-§50, `docs/decisions.md` D-027 … D-034.

STATE (2026-09-21 evening). Staging pass 11 tied out on both paths: **1,048 entries, $240,844.35, $0.00 on all
ten properties, 922 linked - every link a Drive file** (`drive-filing.json`: 423 keys, 636 files; `evidence/`
filed). 33 advances, rehearsed with the one-rebuild-per-property registration (7.5 min). Corrections C-1 … C-33.
Staging = the dry run in `data/migration/2026-09-17/rows/`; the staging project = the repo (verified by pull).

FACTS CORRECTED 09-21: **881 Newport has NOT closed - under contract** (like Ashburne: still held). Sold =
1616 Granite and 280 Sparkling only; the old `Sales` tab lists projections for Newport and Ashburne. No
settlement date is owed for cutover. pvb421@ was listed in full and never read; four February forwards to
104ashburne@gmail.com bounced - two rows linked from them, two fan orders parked (audit §48).

TOOLS: tie-out `python3 scripts/migration-journal-tieout.py <dir with tab-Journal.json, tab-Advances.json>
data/migration/2026-09-17/rows` (exit 0 = clean); filing `scripts/migration-file-docs.mjs` (Paul starts it:
`WRITER_SECRET` is masked on Netlify; `env:get WRITER_URL` needs `--context production`). Claude can rename
files in Paul's Drive and search it, not create files there. After `migrationRunStaging` the `books-cache`
snapshot lags up to five minutes - check `posted_at`, not only `fetchedAt`.

NEXT: cutover day with Paul, from `docs/cutover-runbook.md` §1. Note its one reordering: `WRITER_URL` flips to
production BEFORE the production tie-out (the snapshot is read through it). Nothing received 09-11 … 09-17
needs replaying (all in the migration or settled); live mail since 09-18 (an Uber $33.30 of 09-18/19 is
pending in the Inbox) and what Paul held back (Mesa Central States $49.54, the HILCO bill) go in after the flip.
Known writer quirk: about 1 `storeDocument` reply in 12 came back `ok` without its url during the bulk filing
(file created all the same) - cause not found; watch for it in live filing after cutover.

Small, open: list 2 still shows the three rows Paul dropped; VistaPrint 06-21 shipping ($50.40) is in neither
row. Owed by others: Dennis's PayPal receipts for the Granite, Sparkling and Newport listing fees.
