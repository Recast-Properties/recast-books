# Handoff — Recast Books, end of 2026-09-15

Paste this into a new Claude Code session opened at `~/Desktop/Claude` (or directly in
`~/Desktop/Claude/Recast-books`).

---

You are continuing the build of **Recast Books**, the new bookkeeping system for Recast
Properties LLC, in the repo `~/Desktop/Claude/Recast-books/`. Start by reading, in order:
`Recast-books/CLAUDE.md` (status + load-bearing constraints), `docs/decisions.md`
(D-001..D-022 — never reverse one without a new entry), `docs/phase2.6-spec.md` §5 (the
property tab as Paul signed it off), `docs/phase3-spec.md` (awaiting Paul's go), and the
last three `CHANGELOG.md` entries. My memory file `recast-books-build` has the live ids
and gotchas. `HANDOFF-2026-09-14.md` is the day-by-day log for the 14th and 15th.

**Where things stand (2026-09-15 evening)**
- Phases 0, 1, 2, 2.5, 2.6 built. The receipts bookkeeper is **live in parallel** for
  receipts@/travel@ mail since 2026-09-11, posting to the new workbook only. The old
  system in `../Recast-site/` runs untouched and must never be modified.
- **Plaid is dropped (D-019).** Phase 3 is banking from statement uploads (OFX/QFX first,
  CSV/PDF fallback) → Feed tab → Claude matching → per-account reconciliation.
  `docs/phase3-spec.md` is written; Paul has not yet said go. Do not start Phase 3 code
  without it, and never re-pitch Plaid or any aggregator.
- **Anthropic costs by workload (D-018):** Console workspaces Recast Books / Receipts (old
  site) / Title Search / Anything else, each system on its own key; top-ups post to 1520
  Prepaid API credits; `/api/api-costs` expenses usage monthly by workspace (Settings page
  section to preview/post; paul@ poller calls it on the 2nd); `ANTHROPIC_ADMIN_KEY` is set.
  The old Personal "Recast Books" key (expires 2026-10-11) can be deleted once the Recast
  Books workspace shows spend.
- **Property tab, signed off by Paul today** (`docs/phase2.6-spec.md` §5): the forecast
  while held — spacer col A; summary (Purchase Principal + Interest, Cash Advance
  Interest, Rehab, Utilities, prorated tax from `Properties.tax_annual`), Profit Breakdown
  with a typed Sale Price (kept across rebuilds), Dennis Share / Paul Share at
  `Properties.dennis_share_pct` (default 50; 0 when Dennis is the bank only), Payouts;
  Dennis block with two schedules (purchase principal on top, cash advances below the
  who-paid blocks), End Date typed on the tab and written to `Advances.repaid_date` by the
  writer's `onPropertyTabEdit` trigger; Rehab Costs and Utilities blocks to row 305 with
  checkboxes; Paul's greens `#a3f67f` / `#ceffbc`. Rebuild any time with
  `rebuildAllPropertyTabs()` in the writer editor. The **closing tab** (settlement actuals,
  Dennis interest true-up, payouts = net proceeds, post-sale costs, estimate vs actual) is
  Phase 5, with the sell wizard (BUILD-PLAN §5).
- **Advances (D-022):** kinds `purchase` (posts as the purchase, Dr 1000 / Cr 2010 —
  Dennis pays the seller directly) and `cash` (lands in a bank account, or on 2030 "Paul
  Personal" when Paul deposited the check); each advance has its own `rate_pct` (default
  Settings 8%). **Every advance is against a property and its interest is that property's
  cost, split through the shares (D-011, D-021).** A "personal loan" kind and a "Paul pays
  cash-advance interest" rule were both tried and withdrawn today (D-020, D-022 addendum);
  Paul finds this confusing — the rule is "interest follows the money". 1616 Granite carries
  its three real advances; the tab reconciles to the cent on a $430,000 sale estimate.
- **Open items:** Paul's go on Phase 3; the 2.6 gate steps for Granite's mailbox
  (properties@ poller setup, dry run, live); Paul still owes Dennis's and the accountant's
  emails for Users; W-9 collection (Jan 31); Ashburne will need `dennis_share_pct = 0` at
  migration.

**How Paul wants to work**
- **One step at a time.** When Paul must act (console, Apps Script editor, Terminal,
  browser), give exactly one step and wait for "done". Lists annoy him.
- He runs deploys and secrets himself: `npm run deploy` (the classifier blocks it for
  Claude), `npx netlify-cli env:set NAME "value" --secret --context production` (the
  `--context` flag is required or nothing is set; then redeploy).
- Confirm plans before code; write `docs/phaseN-spec.md`; build with Sonnet agents from
  the spec; review every file; `npm test` (407 tests); gate through Paul's browser (he signs
  in on the netlify.app origin — the custom domain has no session).
- Claude is the bookkeeper: model judgment decides; code owns arithmetic, balanced
  entries, txn_id identity, gates. A fact only Paul knows is held for him, never guessed.

**Operating cheatsheet**
- Site: `cd ~/Desktop/Claude/Recast-books && npm run deploy` (Paul runs it).
- Writer: `cd apps-script/writer && clasp push -f && clasp deploy -i AKfycbwRH2juzUkuOzmltG79lbWyQ5CH-Q_cZukY7WcvspOwIw8EpCTnbjQforU-A-q2Jljy`
  (currently @38). Manifest scope changes need Paul to re-run `setup()` in the editor.
  If clasp says `invalid_grant`, Paul runs `npx clasp login`.
- Poller: `cd apps-script/poller && clasp push -f` (no deploy).
- Writer editor helpers: `setup()`, `setupTotals()`, `rebuildAllPropertyTabs()`,
  `installTriggers()`.
- Workbook reads from Claude: open the sheet in Paul's Chrome (claude-in-chrome); small
  data edits there are fine, code goes through the writer.
- Prompt lives in `lib/bookkeeper-prompt.md`; after editing run `node scripts/build-prompt.mjs`.

**First thing to do in the new session:** ask Paul whether he has read
`docs/phase3-spec.md` and wants to go, or whether there is anything else on the property
tab or the Dennis page first. If go: build Phase 3 from the spec with Sonnet agents (copy
`books-upload.mjs` / `books-ingest-background.mjs` patterns and the Phase 2 poller — copy
working code, don't re-spec it), review, test, then gate on one real Citizens month.
