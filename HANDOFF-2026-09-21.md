# Handoff, 2026-09-21 - Recast Books Phase 4 migration: lists done, staging Run and Drive filing next

This is the prompt for the next session. It replaces `HANDOFF-2026-09-18.md`.

---

You are picking up the Recast Books Phase 4 migration. Work only in
/Users/paulbjork/Desktop/Claude/Recast-books/ (its own git repo, branch master, **no remote**). Never touch
the old workbook, ../Recast-site/, or the production workbook/writer without Paul saying so.

READ FIRST, in this order: Recast-books/CLAUDE.md (the status block is current), then docs/phase4-audit.md
§21 (the pipeline), §13 (corrections register C-1 … C-32), §38-§43 (what changed on 09-18 evening and
09-21), docs/decisions.md D-027 … D-034. Every answer of Paul's and every link / refusal / drop / addition /
retraction / settled document lives in data/migration/2026-09-17/paul-answers.json (keys: paid_from, drop,
link, no_link, redate, add, retracted, confirmed_as_is, no_document_accepted, mail_settled,
differences_settled, rename_payee). Outside evidence he supplied is in data/migration/2026-09-17/evidence/
(see its README).

STATE. Row-driven migration (D-029): every old row is posted as Paul typed it, or at a registered correction,
with its receipt linked. Dry run: **1,048 entries, $240,844.35**, ten property totals
(data/migration/2026-09-17/rows/expected.json), all build in the posting engine; **920 linked (87.8% of rows,
78.6% of dollars)**; 128 rows / $51.4K have no document - $36,880 of it accepted by Paul (contractor checks
and cash), the rest cash labor and small rows, all for the bank statements in Phase 3. 33 advances. All five
review lists are done: in mail not in the books 0, differences 0, confirm 0 real (the 3 shown are rows Paul
dropped), questions answered. Staging passes 1-9 tied out to the cent; staging currently HOLDS pass 9
($240,639.05). The current dry run and writer ARE PUSHED to the staging project (verified by clasp pull on
09-21) and have NOT been run.

FIRST JOB - one step for Paul, then tie out. Ask Paul to run `migrationRunStaging` in the staging editor:
https://script.google.com/home/projects/1Hh0ppVeZepu8GClShAINt4bxNZNNxW1dzZK_xFdmlh6hQ5fCtqdwvtL5/edit
(about 2.5 minutes; it refreshes the Properties rows itself; `migrationRegisterAdvances` is NOT needed - the
advances list did not change). Then tie out: expect 1,048 entries, $240,844.35, $0.00 difference on all ten
properties, txn ids / amounts / payees / doc_urls identical to rows/entries.csv, 920 linked, 33 advances, no
orphans, debits = credits. If a push is needed again, VERIFY IT LANDED with `clasp pull` into a scratch
folder and `cmp` - on 09-18 Paul ran 90 seconds before a push and posted the previous dry run.

HOW TO TIE OUT (read-only, free): npx netlify-cli blobs:get books-cache "tab/Journal" > <scratch>/tab-Journal.json
(also tab/Advances, tab/Accounts, tab/Properties, tab/Periods); check fetchedAt is after Paul's run; compare
migration-* txns by property with expected.json and per txn with rows/entries.csv. Record it in
docs/phase4-audit.md and rows/journal-tieout.json. Second path: scripts/migration-audit-indep.py (run from
data/migration/2026-09-17/) re-parses the old xlsx with no shared code; every difference must be a register
line.

THE PIPELINE (deterministic, free, minutes, runs OFFLINE - the 968 envelopes are cached in
.cache/envelopes/env/, git-ignored): python3 scripts/migration-compare.py --env .cache/envelopes/env --inv
data/migration/2026-09-17 --out data/migration/2026-09-17/comparison  →  python3 scripts/migration-rows.py
--inv … --cmp …/comparison --out …/rows  →  node scripts/migration-rows-check.mjs <dir with tab-*.json>
…/rows  →  python3 scripts/migration-audit-links.py .cache/envelopes (both sections must be clean; Shalom
Granite's second payment is the one expected line)  →  copy apps-script/writer/*.gs, *.html,
appsscript.json and rows/MigrationData.gs into a scratch folder whose .clasp.json has the STAGING scriptId
(1Hh0ppVeZepu8GClShAINt4bxNZNNxW1dzZK_xFdmlh6hQ5fCtqdwvtL5), clasp push -f, clasp pull to verify  →  Paul
runs migrationRunStaging. Never keep MigrationData.gs in the repo's writer folder. `clasp` needs
`npx clasp login` (Paul, in his Terminal) every few days - error "invalid_grant / invalid_rapt". If .cache is
gone: npx netlify-cli blobs:list books-docs --json, then blobs:get each doc/… key (~10 min, 8 in parallel).

STANDING RULES - each was paid for with a mistake:
1. Never tell Paul something is "not in the old books" or has "no document" before searching the amount on
   every old tab WITHIN TWO CENTS, AS A SUM OF THE RECEIPT'S ITEMS, AND AS THE DOCUMENT'S PRE-TAX SUBTOTAL,
   and in ALL LISTED MAIL (gmail-listing-{paul,properties,pvb421}-2026-09-17.json; fields amts, subj, head) -
   not only the 968 documents that were read. When a payment is a reimbursement to a person, look for a store
   receipt of the same amount (Vega's $379.18 Zelle was the Home Depot row "Stair Moldings" typed at the
   subtotal - a double count made and caught the same evening, audit §38). The old tabs' SUMMARY BLOCKS hold
   costs too (Ashburne's $16,031.25 property tax sat in cell E10, C-24).
2. Paul's own words - email subject, text above a forward, handwritten notes, Zelle memos - are evidence, and
   for cash labor they ARE the document. Look for them before asking him who paid or which property.
3. After any change to the inventory, the matcher or paul-answers.json: rerun the pipeline and both audit
   scripts and DIFF THE LINKS AGAINST THE LAST COMMIT; read every link that disappeared before moving on.
4. A refusal in paul-answers.json names its document (docId); never refuse a row outright in bulk. Append
   inventory rows and `add` entries, never insert or delete (answers are keyed by row index, additions by
   list index - retract with "retracted": "<why>" in place).
5. A SOLD property's tab (Granite, Sparkling - NOT Newport: under contract, Paul 09-21, audit §47) migrates exactly as it was closed; anything found
   wrong afterwards is a Cost Recapture line naming the property (D-031 extended). **104 Ashburne has NOT
   closed - it is still held**; corrections go on its own tab. Dennis has no direct payments on Ashburne -
   only cash advances and the purchase loan (D-032). The sale side is Phase 5 (D-033). Property tax: D-034.
6. Claude decides the obvious cases and records the reason; Paul gets only real questions, ONE AT A TIME, with
   the evidence shown, and exactly one step when he must act. He accepts reconciling to a vendor's, utility's
   or bank's own payment history as the guide (TXU, Atlas Pools, his Chase Zelle list).
7. **Stopping rule (Paul, 09-21):** no new cost unless it is proven paid AND absent from the old books by
   total, subtotal and items; otherwise park it for Phase 3. A row is corrected to its receipt only when Paul
   says so (C-20, C-29, C-32 - he has said yes every time a receipt proved a slip).
8. clasp push to staging is fine without asking; npm run deploy is Paul's step; nothing goes to the
   production writer or workbook before cutover. Commit each step to this repo. When Paul's connection is
   poor, work offline and say what was not pushed.

OPEN, in the order to take them:
1. The staging Run and tie-out above.
2. Item 7 - Drive filing. 408 linked documents still carry a Gmail link (Gmail links do not open across
   accounts); 17 linked paul@ messages were never read by the bookkeeper (link by Gmail id; ~$0.21 each to
   read, ask Paul before spending); and data/migration/2026-09-17/evidence/ MUST be filed - nine screenshots
   (Atlas portal x2, TXU history x2, Chase Zelle x5), the Luxury 4 Less invoice PDF, the Chinos estimate text.
   This repo has no remote, so this Mac holds their only copy. Entries flagged EVIDENCE:<file> (C-26, Vega's
   $350 labor) get their doc_url from that filing. Staging Drive folder: docs/phase0-spec.md §10. Design the
   job as resumable; nothing exists yet.
3. Cutover day (D-025), one step at a time with Paul: production writer clasp push + clasp deploy -i; Newport's
   settlement date (owed by Paul); clearBooks → migrationRegisterProperties → migrationRegisterAdvances (the
   skip-rebuild flag is pushed to staging but has never run - rehearse it in staging first) → the same pass →
   tie-out on both paths → replay of the live receipts Paul has held since 2026-09-11 (incl. the Mesa Central
   States $49.54 of 09-18 and the HILCO bill) → WRITER_URL back to production (npm run deploy, Paul) → old
   workbook re-exported, diffed against the 09-17 snapshot, set read-only.
4. Small, optional: list 2 still shows three rows Paul dropped (it is built before drops); the Ashburne
   "Kitchen Faucet" row is typed 04-10 and its receipt is 08/10 (month slip, date left as typed); the
   VistaPrint 06-21 order's $52.98 shipping is in neither row.
Owed by others: Dennis's PayPal receipts for the Granite, Sparkling and Newport $299 listing fees. Parked for
Phase 3: the Cotality invoices (Paul cannot see their payment history), 26 + 66 receipts with items Paul did
not enter, the no-document rows. A separate session handles the "read Paul's subject and notes" prompt gap in
Recast-site/.
