# Handoff — Recast Books Phase 4 migration cleanup (written 2026-09-18, end of day)

Paste everything below the line into a new session started in `/Users/paulbjork/Desktop/Claude`.

---

You are picking up the Recast Books Phase 4 migration cleanup. Work only in
`/Users/paulbjork/Desktop/Claude/Recast-books/` (its own git repo, branch `master`, no remote). Never
touch the old workbook, `../Recast-site/`, or the production workbook/writer without Paul saying so.

**Read first, in this order:** `Recast-books/CLAUDE.md` (the status block is current), then
`docs/phase4-audit.md` §21 (the pipeline) and §22–§27 (today), §13 (corrections register C-1 … C-19),
`docs/phase4-independent-audit.md`, and `docs/decisions.md` D-027 … D-033. Paul's answers and every
link / refusal / drop / addition live in `data/migration/2026-09-17/paul-answers.json`; outside evidence he
supplied is in `data/migration/2026-09-17/evidence/` (see its README).

**State.** Row-driven migration (D-029): every old row is posted as Paul typed it, with its receipt
linked. Dry run at commit `e595739`+: **1,031 entries, $220,628.06**, all build in the posting engine;
868 linked (84.2% of rows, 74.7% of dollars); $36,880 of contractor checks/cash accepted by Paul with no
receipt. Staging passes 3 and 4 tied out to the cent. The current dry run is pushed to the STAGING Apps
Script project and **Paul was running `migrationRunStaging` when this was written - first job: tie it
out.** Expect 1,031 entries, $220,628.06, $0.00 difference on every property
(`data/migration/2026-09-17/rows/expected.json`), 33 advances with no orphans.

**How to tie out (read-only, free):**
`npx netlify-cli blobs:get books-cache "tab/Journal" > <scratch>/tab-Journal.json` (also `tab/Advances`,
`tab/Accounts`, `tab/Properties`, `tab/Periods`), check `fetchedAt` is after Paul's run, then compare
`migration-*` txns by property with `expected.json` and the txn ids with `rows/entries.csv`. Second path:
`scripts/migration-audit-indep.py` (run from `data/migration/2026-09-17/`) re-parses the old xlsx with no
shared code; every difference must be a register line.

**The pipeline (deterministic, free, minutes):** `scripts/migration-compare.py --env <envelopes> --inv
data/migration/2026-09-17 --out …/comparison` → `scripts/migration-rows.py --inv … --cmp …/comparison
--out …/rows` → `node scripts/migration-rows-check.mjs <dir with tab-*.json> …/rows` → copy
`apps-script/writer/*.gs`, `*.html`, `appsscript.json` and `rows/MigrationData.gs` into a scratch folder
whose `.clasp.json` has the STAGING scriptId (`docs/phase0-spec.md` §10, begins `1Hh0pp`), `clasp push -f`
→ Paul runs `migrationRunStaging` (and `migrationRegisterAdvances` first only if the advances list
changed; it takes 14 min until it gets a skip-rebuild flag). Never keep `MigrationData.gs` in the repo's
writer folder. **The envelopes and the scratch folders were in the previous session's scratchpad and are
gone:** re-download all 968 (`npx netlify-cli blobs:list books-docs --json`, then `blobs:get` each
`doc/…` key, ~10 min, 8 in parallel) before running the matcher or `scripts/migration-audit-links.py <dir>`
(expects `<dir>/env/*.json`).

**Standing rules - each was paid for with a mistake today:**
1. Never tell Paul something is "not in the old books" or has "no document" before searching the amount
   on every old tab **within two cents and as a sum of the receipt's items**, and in **ALL listed mail**
   (`data/migration/2026-09-17/gmail-listing-{paul,properties,pvb421}-2026-09-17.json`, fields `amts`,
   `subj`, `head`) - not only the 968 documents that were read. Bank notices count (CNB Zelle "was sent"
   emails carry Paul's memo).
2. Paul's own words - email subject, text above a forward, handwritten notes, Zelle memos - are evidence.
   Look for them before asking him who paid or which property.
3. After any change to the inventory, the matcher or `paul-answers.json`: rerun both audit scripts and
   **diff the links against the last commit; read every link that disappeared** before moving on.
4. A refusal in `paul-answers.json` names its document (`docId`); never refuse a row outright in bulk.
   Append inventory rows, never insert (answers are keyed by row index).
5. A sold property's tab migrates exactly as it was closed; anything found wrong afterwards is a **Cost
   Recapture** line naming the property (D-031 extended). Ashburne closed on its cash draws, so a double
   entry there is a plain drop. Dennis has no direct payments on Ashburne - only cash advances and the
   purchase loan (D-032). The sale side is Phase 5 (D-033).
6. Claude decides the obvious cases and records the reason; Paul gets only real questions, **one at a
   time**, with the evidence shown, and exactly one step when he must act. He accepts reconciling to a
   vendor's or utility's own payment history as the guide (TXU, Atlas Pools).
7. `clasp push` to staging is fine without asking; `npm run deploy` is Paul's step; nothing goes to the
   production writer or workbook before cutover. Commit each step to this repo.

**Open, in the order I would take them (§27):**
1. Tie out the staging run above; record it in `phase4-audit.md` and `rows/journal-tieout.json`.
2. Item 5 - 168 documents in mail and not in the books: utilities first (pair bill + payment, tie to a
   property by service address, post-sale → Cost Recapture, drop what the books already carry), one short
   table per property for Paul. Ask him for the utility's payment history when a property does not tie.
3. Item 4 - 117 receipts whose total differs from their rows (D-028: return, omitted item, or a typed
   amount to correct). Receipt `d3b6197026` (HD 02-19, $390.12) is over-claimed and two rows that belong
   on it (saw blade $54.09, wire connectors $11.70) are not.
4. Fold the all-listed-mail search into `migration-compare.py` as a second source of candidates; queue
   the never-read paul@ messages now linked by Gmail id for a read (~$0.21 each) so they can be filed.
5. Skip-rebuild flag for `migrationRegisterAdvances` (rebuild tabs once at the end).
6. Item 7 - Drive filing of every linked document, including `evidence/`; then production writer push,
   sold properties' settlement dates (Ashburne and Newport still owed by Paul), cutover (D-025), which
   also re-exports the old workbook and diffs it against the 2026-09-17 snapshot.
Owed by others: Dennis's PayPal receipts for the Granite, Sparkling and Newport $299 listing fees. A
separate session (task "Make old receipts poller read Paul's subject and notes") is handling the same
prompt gap in `Recast-site/`.
